### Operating Rules

1. **Check blocks before acting** — the `<!-- pmd: -->` sections below have live project data.
2. **Match existing code style** — imports, naming, formatting, conventions. One concern per edit.
3. **Verify after edits** — run lint/typecheck/test (check `deps` block for commands).
